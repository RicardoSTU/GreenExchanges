// script.js

function login() {
    // Assuming a successful login for demonstration purposes
    // You may need to add actual authentication logic here

    // Redirect to another page (replace 'newPage.html' with your desired page)
    window.location.href = 'info.html';

    // Prevent the form from submitting (since we're handling the redirection via JavaScript)
    return false;
}

